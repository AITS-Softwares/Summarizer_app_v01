# Demo-only UI gate for the administration page. This value is bundled into the frontend,
# so it is not a real security control. Use backend authentication for real access control.
VITE_ADMIN_ACCESS_CODE=demo-admin
