# Backend

The active ASP.NET Core API is in `Summarizer.Api/`.

It uses Entity Framework Core with SQL Server and supports IIS or Docker deployment. Provider credentials are encrypted with ASP.NET Core Data Protection. Persist and protect the data-protection key directory in production.
