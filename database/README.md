# Database

The application uses SQL Server through Entity Framework Core migrations stored under:

`backend/Summarizer.Api/Data/Migrations`

`01-create-database.sql` is provided for manual SQL Server setup. Runtime MDF/LDF files and backups should remain outside source control.
